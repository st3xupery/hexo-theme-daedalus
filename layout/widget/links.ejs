<% if (site.posts.length) { %>
    <div class="widget-wrap widget-list">
        <h1 class="widget-title"><%= __('widget.links') %></h1>
        <div class="widget">
            <ul>
                <% for (var i in theme.miscellaneous.links) { %>
                    <li>
                        <a href="<%- theme.miscellaneous.links[i] %>"><%= i %></a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
<% } %>
